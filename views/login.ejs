<!DOCTYPE html>
<html>
<head>
    <title>Login - Social Demo</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 40px 20px; }
        .login-options { display: flex; flex-direction: column; gap: 10px; margin-top: 20px; }
        .btn { display: block; padding: 15px; text-align: center; color: white; text-decoration: none; border-radius: 5px; font-size: 16px; }
        .btn.google { background: #db4437; }
        .btn.facebook { background: #4267B2; }
        .btn.home { background: #6c757d; margin-top: 20px; }
    </style>
</head>
<body>
    <h1>Choose Login Method</h1>
    
    <% if (typeof error !== 'undefined') { %>
        <div style="background: #f8d7da; color: #721c24; padding: 10px; border-radius: 4px; margin: 10px 0;">
            Login failed. Please try again.
        </div>
    <% } %>
    
    <div class="login-options">
        <a href="/login/google" class="btn google">Continue with Google</a>
    </div>
    
    <a href="/" class="btn home">Back to Home</a>
</body>
</html>